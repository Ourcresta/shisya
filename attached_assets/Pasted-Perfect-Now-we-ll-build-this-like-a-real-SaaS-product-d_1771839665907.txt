Perfect. Now we’ll build this like a **real SaaS product design system**, not just random colors.

Below is your **Our Shiksha – Complete Theme Design System (Dark AI Professional)**
✅ Ready to copy
✅ Tailwind-ready
✅ Scalable
✅ Role-based accent system

---

# 🌌 OUR SHIKSHA – THEME DESIGN SYSTEM

Version 1.0 – AI Career Operating System

---

# 1️⃣ 🎨 COLOR SYSTEM

## 🔹 Core Background Colors

```js
// tailwind.config.js
theme: {
  extend: {
    colors: {
      // Backgrounds
      bgPrimary: "#0B1120",
      bgSecondary: "#111827",
      bgTertiary: "#1F2937",

      // Glass Cards
      glassLight: "rgba(255,255,255,0.05)",
      glassBorder: "rgba(255,255,255,0.08)",

      // Text
      textPrimary: "#F9FAFB",
      textSecondary: "#9CA3AF",

      // Accent - Default (Shishya)
      accent: "#22D3EE",
      accentHover: "#06B6D4",

      // Guru Accent
      guruAccent: "#8B5CF6",

      // Admin Accent
      adminAccent: "#10B981",

      // Status Colors
      success: "#22C55E",
      warning: "#F59E0B",
      danger: "#EF4444",
    }
  }
}
```

---

# 2️⃣ 🌈 GRADIENT SYSTEM

Use for hero sections and highlights.

```css
.bg-ai-gradient {
  background: linear-gradient(
    135deg,
    #0B1120 0%,
    #111827 50%,
    #0F172A 100%
  );
}

.bg-accent-gradient {
  background: linear-gradient(
    90deg,
    #22D3EE,
    #06B6D4
  );
}
```

---

# 3️⃣ 📏 SPACING SYSTEM (Consistent SaaS Layout)

Use 8px base grid.

```js
spacing: {
  xs: "4px",
  sm: "8px",
  md: "16px",
  lg: "24px",
  xl: "32px",
  "2xl": "48px",
  "3xl": "64px"
}
```

### Section Padding Standard

```html
<section class="py-20 px-6 md:px-12 lg:px-24">
```

---

# 4️⃣ 🔤 TYPOGRAPHY SYSTEM

Use Inter or Sora.

```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
```

### Base Styles

```css
body {
  font-family: 'Inter', sans-serif;
  background-color: #0B1120;
  color: #F9FAFB;
}
```

### Headings

```css
.h1 { font-size: 48px; font-weight: 800; }
.h2 { font-size: 36px; font-weight: 700; }
.h3 { font-size: 24px; font-weight: 600; }
```

---

# 5️⃣ 🧊 CARD SYSTEM (Glassmorphism)

## 🔹 Glass Card Component

```html
<div class="glass-card p-6 rounded-2xl">
  Content here
</div>
```

```css
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(15px);
  border: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 0 40px rgba(34, 211, 238, 0.08);
  transition: all 0.3s ease;
}

.glass-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 0 60px rgba(34, 211, 238, 0.15);
}
```

---

# 6️⃣ 🔘 BUTTON SYSTEM

## 🔹 Primary Button (Shishya)

```html
<button class="btn-primary">Start Learning</button>
```

```css
.btn-primary {
  background: linear-gradient(90deg, #22D3EE, #06B6D4);
  padding: 12px 24px;
  border-radius: 12px;
  font-weight: 600;
  color: #0B1120;
  transition: all 0.3s ease;
}

.btn-primary:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px rgba(34, 211, 238, 0.6);
}
```

---

## 🔹 Guru Button Variant

```css
.btn-guru {
  background: linear-gradient(90deg, #8B5CF6, #7C3AED);
  color: white;
}
```

---

## 🔹 Outline Button

```css
.btn-outline {
  border: 1px solid #22D3EE;
  color: #22D3EE;
  padding: 12px 24px;
  border-radius: 12px;
  transition: 0.3s;
}

.btn-outline:hover {
  background: #22D3EE;
  color: #0B1120;
}
```

---

# 7️⃣ 🧭 NAVBAR SYSTEM

```html
<nav class="navbar">
  Logo | Links | Profile
</nav>
```

```css
.navbar {
  background: rgba(11, 17, 32, 0.7);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(255,255,255,0.08);
}
```

### Hover Effect

```css
.nav-link {
  position: relative;
}

.nav-link::after {
  content: "";
  position: absolute;
  width: 0%;
  height: 2px;
  bottom: -4px;
  left: 0;
  background: #22D3EE;
  transition: 0.3s;
}

.nav-link:hover::after {
  width: 100%;
}
```

---

# 8️⃣ 📊 DASHBOARD SIDEBAR SYSTEM

```css
.sidebar {
  background: #111827;
  border-right: 1px solid rgba(255,255,255,0.08);
}

.sidebar-item {
  padding: 12px 16px;
  border-radius: 8px;
  transition: 0.3s;
}

.sidebar-item:hover {
  background: rgba(34, 211, 238, 0.1);
}

.sidebar-active {
  background: rgba(34, 211, 238, 0.2);
  border-left: 3px solid #22D3EE;
}
```

---

# 9️⃣ ⚡ ANIMATION SYSTEM

## 🔹 Fade In

```css
.fade-in {
  animation: fadeIn 0.8s ease forwards;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
```

---

## 🔹 Glow Pulse

```css
.glow-pulse {
  animation: glowPulse 2s infinite alternate;
}

@keyframes glowPulse {
  from { box-shadow: 0 0 10px #22D3EE; }
  to { box-shadow: 0 0 30px #22D3EE; }
}
```

---

## 🔹 Floating Animation (AI Background)

```css
.float {
  animation: float 6s ease-in-out infinite;
}

@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}
```

---

# 🔟 ROLE-BASED ACCENT SYSTEM

In React:

```js
const roleAccent = {
  shishya: "#22D3EE",
  guru: "#8B5CF6",
  admin: "#10B981"
};
```

Apply dynamically to:

* Buttons
* Progress bars
* Sidebar highlight
* Charts

---

# 🏆 DESIGN PRINCIPLES FOR OUR SHIKSHA

✔ Dark AI Professional
✔ Minimal glow
✔ Consistent spacing
✔ 8px grid system
✔ Role-based accent
✔ Glass over flat
✔ Smooth transitions

---

# 🚀 FINAL RESULT

With this design system, Our Shiksha will look like:

* AI SaaS platform
* Internship operating system
* Career engine
* Not a normal EdTech site

---

If you want next, I can generate:

* 🔥 Complete Tailwind config file
* 🔥 Component library (React reusable components)
* 🔥 Full homepage layout using this system
* 🔥 Internship dashboard UI layout

Tell me next build step.
